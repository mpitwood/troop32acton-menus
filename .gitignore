node_modules/
.claude/

# Generated by `npm run build` -- GitHub Actions builds and deploys this
# fresh on every push (see .github/workflows/pages.yml). Keeping it out of
# git means the Markdown is the only source of truth, with no risk of a
# stale docs/ committed after an edit. Still generated locally for preview.
docs/

# Official BSA requirement PDFs and working source files - not for public redistribution
requirements/First-Class.pdf
requirements/Second-Class-v2.pdf
import/
